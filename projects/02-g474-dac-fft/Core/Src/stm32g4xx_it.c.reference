/**
 * stm32g4xx_it.c — 不需要修改！
 *
 * 之前尝试手动写 DMA 中断代码 → hdma_adc1 找不到 → 踩坑
 *
 * 正确做法：
 *   CubeMX 自动生成的中断函数和 HAL_DMA_IRQHandler() 已经够用。
 *   DMA 完成后 HAL 会自动调用 HAL_ADC_ConvCpltCallback()，
 *   这个回调写在 main.c 里就行（见 main.c.reference）。
 *
 * 本文件保持 CubeMX 生成的原始状态，不做任何修改。
 */

/* ============================================================
 * 总结：不需要动这个文件
 * ============================================================
 *
 * 流程:
 *  DMA1_Channel1_IRQHandler (CubeMX自动生成)
 *    → HAL_DMA_IRQHandler (HAL库)
 *      → HAL_ADC_ConvCpltCallback (我们写在main.c里)
 *
 * 我们只需要在 main.c 里实现 HAL_ADC_ConvCpltCallback 即可。
 * ============================================================ */
